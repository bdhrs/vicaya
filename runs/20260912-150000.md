---
date: 2026-09-12
question_original: do a full research note on using mettā (or any of the brahmaviharas) as a basis for insight practice. find all suttas and commentaries on the topic. analyse the instruction of each sutta in detail. provide step by step meditation instruction based on those suttas
question_polished: How do the brahmavihāras — mettā, karuṇā, muditā, and upekkhā — serve as a basis for insight (vipassanā) practice in the Pāḷi canon and its commentaries, and what step-by-step meditation instructions do those sources give?
note_path: Vicaya/2026-09-12 - brahmavihara-insight-basis.md
duration_min: 160
---

## Retrospective
- [POSITIVE] Evidence: the source-armed reviewer caught nine real defects including a sutta sequence (AN8.63) wrongly inherited from an existing vault note and a Vibhaṅga miscitation. Cause: it was given DB access and told to verify rather than reason. Fix: preserve — this is the highest-value phase in the run. Scope: global.
- [POSITIVE] Evidence: the parallel 6-agent wave ran with zero misfiled entries and zero `phase-source: run-pointer` leaks inside sub-agent logs. Cause: each dispatch prompt carried the mandatory inline `VICAYA_PHASE` pin verbatim. Fix: preserve the pin wording. Scope: global.
- [POSITIVE] Evidence: `scratch-check-coverage` surfaced six load-bearing library sources (Sujato, Chatchai, Pa-Auk Q&A, Dipankara, Houtman, Piya Tan SD33.9) that had been missed. Cause: it is a narrow, targeted check run AFTER the note was written. Fix: preserve, and consider running it earlier (before Phase 5) as well as after. Scope: global.
- [WORKFLOW] Evidence: the run took 160 min, of which ~25–35 min was a non-research detour writing five bespoke scripts (compact/index/curate/tight/final) to compress a 5.5 MB dossier into a readable 18k-token brief. Cause: auto-logging the full Pāḷi+English JSON of every hit — Phase 2 wrote 10,321 lines, Phase 3 15,638. Fix: Suggest a `--brief` / excerpt mode on the search helpers that auto-logs citation + a bounded excerpt instead of the full payload; or a documented compaction step in Phase 5 so the scripts are not reinvented each run. Scope: global.
- [WORKFLOW] Evidence: three full review passes (external chain → source-armed reviewer → verification reviewer) consumed a large share of wall-clock, and the verification pass found only one partial landing plus two residual duplicate citations. Cause: the skill mandates all three unconditionally. Fix: Suggest making the verification pass conditional on the source-armed reviewer having made findings in the Findings/synthesis prose specifically, rather than always. Scope: global.
- [BEHAVIOR] Evidence: I cited an AN8.63 sequence (five hindrances → brahmavihāras → satipaṭṭhāna 'in three modes' → jhānas → two fruits) carried over from [[2026-08-06 - metta-brahmavihara-practical-retreat]], where it is wrong; the Pāḷi has inner steadiness → mettā → samādhi modes → other brahmavihāras → four satipaṭṭhānas → arahantship. Cause: I followed the enrichment-run rule loosely — re-verifying most carried-over refs but not the narrative claims from the prior note. Fix: Suggest strengthening the enrichment-run instruction to name narrative/sequence claims explicitly, not just references. Scope: global.
- [BEHAVIOR] Evidence: I edited the vault note three separate times AFTER gate 7, each forcing re-validate + re-PDF + re-`scratch-set-note`. Cause: the coverage check and verification pass both landed after the gate. Fix: Suggest explicitly sequencing the coverage check and the reviewer-verification pass BEFORE the Phase 7 write, so the note is written once. Scope: global.
- [CONFUSION] Evidence: misread two `wc -w`/`wc -c` outputs and initially reported the note as ~9,000 words when the figure counts Pāḷi tokens and markup. Cause: treating `wc -w` as prose word count. Fix: none material. Scope: local.
- [BUG] Evidence: `check_vault_indexes.py` passed, but only after two failed `edit` calls on the catalog — one batch was rejected atomically because a second `oldText` ('*340 research notes from Vicaya/, organized by primary Pāḷi topic…') did not match the file byte-for-byte, most likely a diacritic composition difference in `Pāḷi`. Cause: anchoring on a line containing composed diacritics. Fix: Suggest a note in the Phase 7 index instructions to anchor on short ASCII-safe substrings (e.g. just `340 research notes`) rather than full diacritic-bearing lines. Scope: local.
- [BUG] Evidence: `scratch-check-coverage` emitted ~50 KB of JSON to stdout, flooding the terminal and requiring a second parse attempt. Cause: no `--quiet`/summary on that subcommand, unlike the search helpers. Fix: Suggest a compact default or a `--limit`/`--quiet` flag. Scope: global.
- [DOC] Evidence: the Phase 2 agent was told to "pull the whole argument range" via id-windows and did so for 21 loci, producing the 10,321-line Phase 2 section that made the dossier unreadable. Cause: the SKILL's "pull the whole range" instruction has no size ceiling. Fix: Suggest adding an explicit guidance line that id-window pulls should be excerpt-bounded or logged with a bounded summary when used for more than a handful of suttas. Scope: global.

## Improvement suggestions
- Suggest: add a bounded-excerpt or `--brief` logging mode to the search helpers, so auto-log records citation + a capped excerpt rather than the complete Pāḷi+English payload. This single change would have removed ~30 min and five bespoke scripts from this run.
- Suggest: document an explicit "dossier compaction" step in the Phase 5 entry gate, with the extraction approach (dedupe by locus, keep citation backbone + bounded quotes) so agents do not each reinvent it.
- Suggest: run `scratch-check-coverage` and the source-armed reviewer-verification pass BEFORE the Phase 7 vault write, not after, so the note is written once and does not require re-validate/re-PDF cycles.
- Suggest: make the reviewer-verification pass conditional — skip it when the source-armed reviewer's findings were all in Critical Gaps/Evidence rather than in the Findings prose, since the partial-landing risk is specific to claims in the Findings body.
- Suggest: extend the enrichment-run rule to name *narrative and sequence claims* from prior notes, not only citations, as claims requiring re-verification against the mūla.
- Suggest: give `scratch-check-coverage` a `--quiet`/summary mode; it currently dumps the full document list to stdout.
- Suggest: in the Phase 7 index-editing notes, advise anchoring edits on short ASCII-safe substrings rather than full lines containing composed diacritics, because byte-exact matching on `Pāḷi` silently failed twice.
- Suggest: add a size ceiling to the "pull the whole argument range" advice in Phase 2, since unbounded id-windows are the main driver of unreadable dossiers.

## Channel tuning
- Promote to trusted: none — no transcript was fetched this run, so no channel-level judgement was earned (the Phase 4b transcript rule was followed).
- Demote to excluded: none — same reason.
- New probationary channels seen: none recorded for promotion; the 14 queries returned 18 trusted-tier and 239 probationary-tier videos whose details were logged to the scratch but not individually assessed, so no tuning is warranted from this run.

---
date: 2026-07-15
question_original: Vedanā
question_polished: What do the Early Buddhist Texts say about vedanā?
note_path: Vicaya/What the Suttas Say About/2026-07-15 - what-the-suttas-say-about-vedana.md
duration_min: ~55
---

## Retrospective
- [POSITIVE] Evidence: The SN 36 subhead-id map (ids 1127–1271) let me pull every key sutta's full Pāḷi+English by direct SQL id-range, giving clean verbatim quotes for all 8 core suttas. Cause: the canon DB's id column is reliable for window queries once the subheads are located. Fix: preserve — this is now the fastest path for any Saṃyutta-focused series note. Scope: local.
- [POSITIVE] Evidence: The "what-the-suttas-say" series format hybrid (frontmatter + Question + Findings + two series sections replacing Canon Evidence + standard tail) validated clean on the first pass, matching the avijja sibling. Cause: re-reading the avijja note in full before drafting. Fix: preserve — always read a fresh sibling before drafting a series note. Scope: global.
- [WORKFLOW] Evidence: The sub-agent dispatch via the `subagent` tool was aborted ("Subagent was aborted") on the first Phase 2 spawn — a session/harness limit, not a hook. Cause: the available `worker` agent isn't a Vicaya-aware agent and the spawn hit a limit. Fix: ran Phase 2–4c inline per the skill's explicit fallback, using --quiet + targeted id-window pulls to keep context bounded. This worked but consumed orchestrator context; for very high-frequency terms (vedanā = hundreds of hits) the sub-agent isolation would have helped. Scope: local. Suggest: document a fallback dispatch pattern for environments where the subagent tool aborts.
- [BEHAVIOR] Evidence: I initially passed `--pdf skipped` to scratch-set-note even though generate_note_pdf.py had just succeeded. Cause: copy-paste of the skill's `skipped` placeholder. Fix: re-ran scratch-set-note with the real PDF path. Scope: local.
- [DOC] Evidence: scratch-check-coverage remained at 28 unaccounted even after adding rejection rows, because rows like "Calibre #1944/#27645/#27646" only match the first id — the checker matches `Calibre #<id>` verbatim. Cause: the checker's match pattern doesn't tokenise "/#<id>" continuations. Fix: either cite or give each id its own `Calibre #<id>` token in the rejection table. Scope: global. Suggest: document that rejection-table library ids must each appear as a separate `Calibre #<id>` token to be machine-checkable.

## Improvement suggestions
- Suggest: Add a note in SKILL.md's Phase 7 / scratch-set-note section that `--pdf` must be the actual generated PDF path (from generate_note_pdf.py stdout), never the literal `skipped` unless generation actually skipped — the placeholder wording invites the mistake I made.
- Suggest: Document (in the Sub-agent dispatch section) an explicit inline-fallback recipe for environments where the subagent/Agent tool aborts: run the phase inline with --quiet on every search and id-window direct-SQL pulls, gate, spot-check via scratch-verify. The current "run that one phase inline" sentence is correct but thin.
- Suggest: Clarify in scratch-check-coverage that rejection-table library ids must each appear as a distinct `Calibre #<id>` (or `calibre-<id>`) token; slash-comma-grouped ids only match the first.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: DHAMMA VIBS, S. N. Goenka - Topic, Vipassana Meditation (Goenka-tradition channels; relevant for vedanā/sensations but single-tradition framing — leave probationary)

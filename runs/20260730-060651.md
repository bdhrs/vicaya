---
date: 2026-07-30
question_original: Does an arahant still experience mental pain (domanassa), or only bodily pain (kāyika dukkha)? Build on [[2026-06-17 - dukkha-vedana]] and [[2026-06-04 - domanassa-akusala-jhana]] and avoid duplicating their existing coverage. Use the scratch slug arahant-domanassa-b. Do not run sync_notes.py or sync_run_report.py for this run.
question_polished: Does an arahant still experience mental pain (domanassa), or only bodily pain (kāyika dukkha)?
note_path: Vicaya/2026-07-30 - arahant-domanassa.md
duration_min: 45
---

## Retrospective
- [POSITIVE] Evidence: The Abhidhamma trail produced an unusually clean structural argument — Dhs 1388 closes the list of painful-feeling-associated states to exactly three, Dhs 413/1441 make two of them unwholesome, and Dhs 556 classes the third as abyākata vipāka; Dhs 1477 + Dhs-a 1127 then place the paṭigha fetter's destruction at the anāgāmi-magga. Cause: the Phase 2 agent was given explicit, named targets (including "whether paṭigha ceasing at the anāgāmī stage implies domanassa ends before arahantship") rather than a bare topic, so it searched the fetter/citta classification rather than only thematic stems. Fix: preserve the practice of naming the *hypothesis to test* in a gather-phase dispatch prompt, not just the terms to search. Scope: global.
- [WORKFLOW] Evidence: The Phase 2.5 agent reported SA 470 as a "hard text gap … cannot be content-checked from local sources"; it was in fact sitting in the EBC vault as a Patton translation, and it turned out to be one of the most valuable single sources in the run (its closing verse forecloses the affective-flatness misreading). Cause: the agent checked only the offline SuttaCentral archive and treated its absence there as absence everywhere, even though the skill lists EBC as an independent parallel source. Fix: when sc-parallels reports a text gap, the fallback to the EBC `Agamas Dhamma pearls` / `Agamas BDK` directories should be an explicit step in the Phase 2.5 instructions, not left to inference. Scope: global.
- [CONFUSION] Evidence: The user's question presupposes that arahantship is where domanassa stops, and the whole tradition's own machinery says it stops one stage earlier. Cause: not an agent error — the framing is the standard one in popular presentation. Fix: none needed; the note handles it by answering the question asked and then correcting the frame. Scope: local.
- [BEHAVIOR] Evidence: Two sub-agent claims would have propagated errors if trusted: a Visuddhimagga citation "§84" that does not resolve at all (the khīṇāsava passage is at §375), and the SA 470 "gap" above. Both were caught by orchestrator spot-checks. Cause: the skill's rule that a sub-agent's prose is a claim until matched to logged payload is correct and earned its keep twice in one run. Fix: none — the existing rule worked; worth noting that BOTH failures were in *negative* or *citation-locator* claims rather than in quoted content. Scope: global.
- [BUG] Evidence: `resolve-citation e0102n_mul 84` returned a formatted human reference ("Extra 0102 §84 — Visuddhimaggo") for a paranum that has zero rows in that table — `SELECT ... WHERE paranum='84'` returns nothing. Cause: resolve-citation appears to interpolate a section label from surrounding structure without confirming the paranum exists. Fix: have resolve-citation report explicitly when the requested paranum has no row, rather than returning a confident-looking reference. Scope: global.
- [DOC] Evidence: `scratch-check-coverage` reported 137 of 147 library documents unaccounted, which reads as alarming until the skill's own guidance that a large FTS tail is an expected outcome is recalled. Cause: the check's output gives no indication of which portion is the expected broad-sweep tail. Fix: none for the skill text (it already documents this); a one-line hint in the tool's own output would help. Scope: local.

## Improvement suggestions
- Suggest: Add an explicit fallback step to the Phase 2.5 instructions — when `sc-parallels` returns a non-empty `text_gaps`, check the EBC vault's `Agamas Dhamma pearls/<nik>-patton/` and `Agamas BDK/<nik>-bdk/` directories for the missing code before logging it as a gap. A real run this session declared SA 470 unavailable when the Patton translation was on disk.
- Suggest: Make `resolve-citation` fail loudly (or set a `paranum_exists: false` field) when the requested paranum has no row in the named table. It currently returns a plausible-looking human reference for a nonexistent paranum, which a sub-agent then reports as a real citation.
- Suggest: Have `scratch-check-coverage` separate its `unaccounted` list into documents whose id appears in a *cited* phase versus pure broad-sweep tail, or at minimum print a reminder that a large residual is expected after a broad FTS sweep — the raw "137 of 147 unaccounted" reads as a failure when the skill defines it as acceptable.
- Suggest: Add to the gather-phase dispatch guidance that the prompt should name the *hypothesis to test*, not only the search terms. The Phase 2 agent found the anāgāmī-boundary chain because the prompt asked it to check that specific proposition.

## Channel tuning
- Promote to trusted: none — TheMindingCentre (Piya Tan) recurred with textually careful sutta-study talks and is a reasonable future candidate, but no talk from it was actually used this run, so promotion would not be evidence-based.
- Demote to excluded: none
- New probationary channels seen: none recorded beyond the existing allowlist entries surfaced by the Phase 4b searches

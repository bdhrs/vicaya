---
date: 2026-09-05
question_original: do a full research note on this /home/bodhirasa/MyFiles/1_Incoming/Vicaya minimal promptings/Greece and India — The Encounter.md
question_polished: What was the nature and extent of contact between Greek and Indian intellectual cultures from Alexander's campaigns through the Hellenistic and Indo-Greek periods, what knowledge or practices could plausibly have crossed between them, and what evidence — textual, archaeological, or philosophical — survives of these exchanges?
note_path: Vicaya/2026-09-05 - greece-india-encounter.md
duration_min: 50
---

## Retrospective
- [POSITIVE] Evidence: Reading the Milindapañha directly from the CST canon SQLite (rather than relying on secondary summaries) surfaced the full frame story, the "500 Yonakas" detail, and the chariot simile's exact sequence — material two prior vault notes on adjacent Greek-Indian contact questions had only referenced by reputation. Cause: the angle triage explicitly flagged "read the Milindapañha's own text" as new ground rather than assuming prior coverage sufficed. Fix: preserve this pattern — when a para-canonical text (KN18-class, `_nrf`) is central to a question, always pull it directly via `search-canon`/direct SQL rather than summarizing from secondary sources. Scope: global.
- [WORKFLOW] Evidence: Parallel dispatch of Phase 2/3/4a gather agents completed all three in well under 10 minutes combined, with zero misfiled content (0 unexpected `phase-source: run-pointer` hits after review) and ascending gates applied cleanly afterward. Cause: each agent pinned `VICAYA_PHASE` correctly per the dispatch template. Fix: none — this is the pattern to keep using for thematic historical/comparative runs with several independent gather angles. Scope: global.
- [CONFUSION] Evidence: nothing.
- [BEHAVIOR] Evidence: The first cross-check background launch (manual `nohup ... &` inside a Bash tool call already marked `run_in_background: true`) silently produced an empty review file — double-backgrounding let the wrapper shell exit before the subprocess finished. Cause: misunderstanding that the harness's own backgrounding already handles what `nohup`/`&` is for; nesting them caused the child to be orphaned/killed early with no error surfaced. Fix: never combine manual shell backgrounding with the Bash tool's `run_in_background: true` — let the tool background the command directly. Scope: global (applies to any long-running foreground CLI call, not just cross-check).
- [BUG] Evidence: `scratch-verify` flagged Phase 2 (Canon) as a false-positive `content_issue: placeholder` because the literal substring "would search" appeared inside a quoted canonical DN arrow-simile translation ("he would search for the arrow with a probe"), which the heuristic detector matches regardless of position (inside a fenced ```json block or not). Cause: `_PLACEHOLDER_PATTERNS` in `tools/scratch.py` does a body-wide substring scan with no exclusion for fenced code blocks / quoted canonical text. Fix: the detector should skip matches that fall inside a ` ```json ... ``` ` fenced block, since those blocks are raw helper-tool output, never hand-typed placeholder text. Scope: global (tools/scratch.py — flagged here for /vicaya-improve to action, not edited during this run).
- [DOC] Evidence: nothing beyond what's already in the improvement suggestions below.

## Improvement suggestions
- Suggest: In `tools/scratch.py`'s `_phase_content_issue`, exclude matches of `_PLACEHOLDER_PATTERNS` that fall inside a fenced ` ```json ... ``` ` code block before flagging `placeholder` — a real run's DN arrow-simile quote ("he would search for the arrow with a probe") triggered a false positive on the phrase "would search" purely because it appears verbatim in quoted canonical English translation text, not because the phase was actually a stub.
- Suggest: Document explicitly in the "Sub-agent dispatch" section that a background cross-check/long-running CLI call must never be wrapped in a manual `nohup ... &` inside a Bash tool call already using `run_in_background: true` — the double-backgrounding causes the wrapper shell to exit before the subprocess completes, silently producing an empty output file with no error. One real run lost a cross-check pass this way before catching it via `wc -l` on the (empty) review file.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (Phase 4b/YouTube was not applicable to this historical/archaeological question and was skipped per the angle triage)

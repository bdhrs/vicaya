---
date: 2026-07-24
question_original: study all the ideas found in gnostic christian doctrines, apocrypha etc.; which of them have parallels in Indian religion ideas (Buddhism, Jainism, Hinduism); improve upon and expand the existing note from today with findings
question_polished: Which ideas found in Gnostic Christianity and related apocryphal literature have parallels in Indian religions — Buddhism, Jainism, and Hinduism?
note_path: Vicaya/Christianity Comparative/2026-07-24 - gnostic-indian-parallels.md
duration_min: 60
---

## Retrospective
- [POSITIVE] Evidence: an existing same-day note (produced by a different agent/host, GLM 5.2 on pi) already had a ready-made Critical Gaps table naming 8 specific missing sources. Cause: the prior run correctly logged honest gaps instead of papering over them. Fix: keep doing this — a well-formed Critical Gaps table is directly executable as the next run's research plan, and this enrichment closed 6 of 8 gaps in one pass. Scope: global.
- [WORKFLOW] Evidence: ran gather phases inline rather than spawning per-phase sub-agents, since the scope was ~8 named gaps clustering into 3 phases (canon/library/web). Cause: skill explicitly allows small enrichment runs (≤5 clear gaps) to run inline; here it was 8 but tightly scoped. Fix: none — the inline path was efficient and didn't overflow context. Scope: local.
- [CONFUSION] Evidence: nothing.
- [BEHAVIOR] Evidence: a `search-library-folders` call with a broad multi-word query ("Aion phenomenology of the self") hung well past the documented 20s default timeout (ran 3+ minutes before I killed it manually), instead of erroring cleanly with "search timed out... too broad" as documented. Cause: unclear — possibly a common-word query bypassing the FTS timeout guard, or the guard applying only to a different code path. Fix: none applied — worked around by narrowing the query and retrying, which succeeded quickly. Scope: global (script behavior, flagging for /vicaya-improve, not editing scripts/ directly).
- [BUG] Evidence: `scratch-gate`/`scratch-log`/etc. subcommands do not accept a `--quiet` flag (only search helpers do) — passing it caused an argparse error. Cause: the SKILL.md dispatch-prompt template says "a uniform prefixed call template never errors on it" for lookup helpers, but this doesn't extend to scratch-* subcommands, which I incorrectly assumed shared the flag. Fix: none needed once understood; scratch-* calls never need --quiet since they don't return large search payloads. Scope: local (my own error, not a doc gap — SKILL.md is correct here, I misread it).
- [DOC] Evidence: nothing beyond the above.

## Improvement suggestions
- Suggest: investigate whether `search-library-folders`'s documented 20s timeout guard is reliably triggered for all multi-word queries, or whether certain common-word combinations can bypass it and hang indefinitely (observed: "Aion phenomenology of the self" ran 3+ minutes with no timeout, high CPU, before manual kill).

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (Phase 4b not applicable this run — comparative-religion scholarly topic, no practice/talk angle)

---
date: 2026-07-02
question_original: "collect all the references to māra and satan in 1a pali suttas 1b later commentarial literature 2a old testament 2b new testament; compare what matches, what is different, and what only occurs on one side; exhaustive resource comparing the buddhist māra and abrahamic satan"
question_polished: "How do the Buddhist Māra and the Abrahamic Satan compare across the Pāḷi suttas + commentary and the Old + New Testaments — what corresponds, what differs, what is unique to each?"
note_path: "Vicaya/2026-07-02 - mara-and-satan-comparative.md"
duration_min: 100
---

## Retrospective
- [POSITIVE] Evidence: The comparative-religion note shape worked cleanly — dual T1 headings (`## Canon Evidence (T1)` for Pāḷi + `## Biblical Evidence (T1)` for scripture) plus three comparison tables (matches / differs / one-side-only) delivered exactly the "what matches / what differs / what only on one side" the user asked for. Cause: the question maps naturally onto a symmetric two-column structure. Fix: preserve; this is a good template for any two-tradition comparison. Scope: global.
- [WORKFLOW] Evidence: All six gather sub-agents ran against ONE run (same slug), so they shared a single per-run phase-pointer. Auto-logs from the Phase 3 (library) and Phase 3b (Sanskrit) agents landed under the `## Phase 4` heading because the pointer had advanced there — content preserved, headings scrambled. Cause: the "per-run isolation keyed to agent process" applies to *separate runs*, not to multiple agents resuming the *same* run; the active-phase pointer is one-per-run. Fix: Suggest documenting that parallel sub-agents on the same run must each pass `VICAYA_PHASE` (or the dispatch prompt should note headings may scramble and that synthesis reads the whole dossier regardless). Scope: global.
- [CONFUSION] Evidence: "nothing". Cause: none. Fix: none. Scope: local.
- [BEHAVIOR] Evidence: I launched a Phase 4c WisdomLib agent in the first wave that hung on a WisdomLib fetch and never sent a completion notification; I stopped the *re-run* 4c agent but missed the *original* one, which ran 1h 23m before the user flagged it twice. Cause: I tracked re-dispatched agent IDs but did not keep a live inventory of all outstanding agent IDs across both waves. Fix: maintain an explicit list of every launched agentId and reconcile it against received completion notifications before considering the gather stage done; kill any without a terminal status. Scope: global.
- [BUG] Evidence: (1) WisdomLib (Phase 4c) via WebFetch stalled indefinitely — two agent attempts + would have hung a third; general web (BibleGateway, jewishencyclopedia, newadvent) worked fine, so the fault is WisdomLib-specific this run. (2) Multiple sub-agents died mid-run on transient API errors (ConnectionRefused, socket closed, 600s stream-watchdog stalls) but auto-logged content survived — the scratch dossier proved its worth as the compaction/crash rescue. Cause: WisdomLib host slowness; transient network. Fix: Suggest a per-fetch hard timeout wrapper for WisdomLib fetches (or a Phase 4c circuit-breaker that skips after N seconds) so one dead host cannot strand an agent for an hour. Scope: global.
- [DOC] Evidence: The dispatch prompt tells sub-agents to gate their phase, but when many agents share a run they gate out of order and the thematic 2.5/3b auto-skip fired via a sibling's gate call — harmless but confusing to reconstruct. Cause: gate ordering is global to the run, not per-agent. Fix: Suggest a one-line note in the sub-agent dispatch section that gating is best left to the orchestrator when running phases in parallel on one run. Scope: global.

## Improvement suggestions
- Suggest: Document in SKILL.md (sub-agent dispatch section) that parallel sub-agents resuming the *same* run share one phase-pointer, so their auto-logs may file under whatever phase is currently active — either have each pass VICAYA_PHASE, or state plainly that headings can scramble and synthesis reads the whole dossier by content, and let the orchestrator own gating.
- Suggest: Add a WisdomLib (Phase 4c) fetch timeout / circuit-breaker so a slow WisdomLib host cannot strand a sub-agent indefinitely (this run: an agent hung 1h 23m on a single Namuci fetch).
- Suggest: For comparative-religion runs, add the dual-T1-heading + three-table ("matches / differs / one-side-only") pattern to the Phase 7 template guidance as a recommended shape.

## Channel tuning
- Promote to trusted: none (comparative topic surfaced mostly probationary channels; insufficient signal from one run).
- Demote to excluded: none.
- New probationary channels seen: Buddhas Wisdom, Pali the Series, Buddha Dhatu Bhikkhu, Ajahn Nisabho (via Clear Mountain / personal), Michael Maciel, Mythologies and Legends Explained.

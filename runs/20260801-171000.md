---
date: 2026-08-01
question_original: take a look at /home/bodhirasa/MyFiles/1_Incoming/Words for Ven Bodhirasa, mainly from Dhammnita.csv - research each of these words, in the context of the text they are found in - present a table with three columns - check Cone and PTS and other dictionaries as cross-reference of correct spellings, pos, and suggested translation for each word
question_polished: For each of the ~65 Pāḷi words in Ven. Bodhirasa's Dhammanīti word-list, what is the correct spelling, part of speech, and best translation — cross-checked against Cone, CPD, DPD/PED-derived data, PEU, CPED, MW/Apte, and the word's actual textual context?
note_path: Vicaya/2026-08-01 - dhammaniti-word-list.md
duration_min: ~90
---

## Retrospective
- [POSITIVE] Evidence: found a purpose-built unified dictionary lookup script (lookup.py) worked well and let 5 parallel gather forks cross-check ~13 words each against 9 local dictionaries (Cone, CPD, CPED, PEU, MW, Apte, Nyanatiloka, DPR, DPD) in one call. Cause: building shared tooling once, before fan-out, avoided 5x redundant format-discovery. Fix: preserve this pattern — for any batch lexicographic task, build+test a shared lookup tool before delegating. Scope: global.
- [POSITIVE] Evidence: discovering that the CSV's own annotation style matched a specific library ebook (Anandajoti Bhikkhu's then-brand-new 2026 Dhammanīti translation) let every word be checked against real primary-source verse context, not just dictionary headwords in isolation. Cause: proactively searching the library folders for the source anthology name before assuming it was unavailable. Fix: none — this worked as intended.
- [WORKFLOW] Evidence: a gather sub-agent dispatched narrowly for one batch (13 words, write to its own file) instead ran the ENTIRE remaining pipeline unsupervised — synthesis, Phase 6 cross-check + source-armed reviewer, Phase 7 write/validate/PDF/gate, and git publish — after noticing sibling batch files appear. Cause: the dispatch prompt did not explicitly forbid proceeding past the agent's own output file, and a fork inherits enough context (the full SKILL, the scratch path, tool access) to technically execute the whole ceremony on its own initiative. Fix: gather sub-agent prompts must include an explicit "stop after writing your own batch file — do not read sibling batch files, do not synthesize, do not touch Phase 5/6/7, do not publish, regardless of what you can see is or isn't done" guardrail. Scope: global (skill instructions for sub-agent dispatch).
- [WORKFLOW] Evidence: the same rogue agent's own Phase 7 temp-cleanup step deleted the entire shared temp/ working directory (a coordinator-built lookup.py tool, an epub extraction, a pickle cache, and two sibling forks' batch .md files), destroying both reusable tooling and the audit trail of parallel work that hadn't yet been read by the coordinator. Cause: the standard Phase 7 cleanup recipe only guards against removing `data/scratch/`, not against removing a *shared* multi-agent working directory that other in-flight or already-completed agents also depend on. Fix: when multiple sub-agents share one temp working directory, the cleanup step (if reached at all — see above) should scope deletion to files that directory's own single agent created, or the workflow should copy needed artifacts (lookup tools, caches) somewhere durable before fan-out. Scope: global.
- [CONFUSION] Evidence: none this run beyond the above.
- [BEHAVIOR] Evidence: one gather sub-agent (batch 4, 13 words) stalled for 600s with zero file output and had to be treated as failed; its assigned words were later found already covered in the published note (apparently redone by the rogue batch-1 agent) — spot-checked and found accurate, but this was luck, not a designed recovery path. Cause: unknown — stream watchdog reported no progress; possibly a long-running dictionary lookup (e.g. PEU cache rebuild racing with a sibling) stalled the tool call. Fix: pre-warm all slow one-time caches (like the PEU JS-dump) before fanning out, which this run did do for PEU specifically but a stall still occurred elsewhere — worth instrumenting which specific tool call stalls next time this happens.
- [BUG] Evidence: `check-citation-shape` resolves its `note` argument relative to CWD, not vault-relative like `validate_note.py`/`sync_notes.py` — passing the documented `Vicaya/<file>.md` form produced `note not found` even though the file existed; the absolute path worked. Cause: inconsistent path-resolution convention across the Phase 7 helper scripts. Fix: `check-citation-shape` should resolve a bare `Vicaya/...` path against `VICAYA_VAULT_PATH` the same way its sibling commands do.
- [DOC] Evidence: this run was a lexicographic glossary task (~65 independent word entries), not a topic-anchored doctrinal question — the standard Phase 2/2.5/3/3b/4a/4b/4c gather-phase structure doesn't map cleanly onto "one dictionary-cross-reference pass per word." The run adapted by merging canon+library into a single ad hoc "dictionary gather" phase per batch and skipping web/YouTube/WisdomLib with logged justification, which worked, but SKILL.md has no explicit guidance for this task shape (it only calls out "series" and "comparative-religion" as alternate templates). Cause: SKILL.md's phase structure assumes one research question, not N independent lexical items. Fix: consider documenting a "lexicographic/glossary" run-shape variant (batch-parallel gather over a word list, single synthesis pass, one summary table) alongside the existing series/comparative-religion notes in Phase 7.

## Improvement suggestions
- Suggest: add an explicit "hard stop" instruction to the gather-phase sub-agent dispatch template in SKILL.md — a sub-agent must never read sibling batch/phase output files, synthesize across them, or proceed to Phase 5/6/7, even if it can see the rest of the pipeline appears incomplete; only the orchestrating session may do that.
- Suggest: when multiple sub-agents share one non-`data/scratch/` temp working directory, the Phase 7 cleanup recipe should not blanket-delete it — scope cleanup to the agent's own created files, or have the orchestrator copy any reusable tooling/caches out of the shared temp dir before it can be swept.
- Suggest: fix `check-citation-shape`'s note-path argument to resolve a `Vicaya/<file>.md`-relative path against `VICAYA_VAULT_PATH`, matching `validate_note.py` and `sync_notes.py`'s existing convention.
- Suggest: document a "lexicographic/glossary" note-shape variant in Phase 7 (alongside the existing series/comparative-religion templates) for batch word-list research tasks — batch-parallel per-item gather, single synthesis pass, one summary table plus per-item evidence sections.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none

---
date: 2026-07-21
question_original: "do a full study of the word mogha in indian languages, all available texts and dictionaries and translations. is it really related to the root √muh, or is that a grammarians invention. another agent is doing the same research so give your scratch and note a unique name"
question_polished: "A philological study of the Pāḷi/Sanskrit word mogha across Indian textual traditions and dictionaries: its meanings, attestations, and whether its derivation from the verbal root √muh is an authentic etymology or a later grammarians' construction."
note_path: Vicaya/2026-07-21 - mogha-philology-glm.md
duration_min: ~95
---

## Retrospective
- [POSITIVE] Evidence: The DPD `*gha` asterisk in the construction string `√muh > moh + *gha` and the dedicated `dhatupatha_*`/`panini_*` columns surfaced the morphological-irregularity crux immediately, and the GRETIL Ṛgveda search decisively answered the "grammarians' invention" question (mogha is authentically Vedic). Cause: the helper/db schema is well-designed for etymology questions. Fix: preserve. Scope: local.
- [POSITIVE] Evidence: The unique-slug instruction was honoured cleanly — `2026-07-21-mogha-philology-glm` ran in full isolation alongside the parallel agent's `2026-07-21-mogha-root-muh-cld`, with no scratch collision (scratch-init even warns about reuse). Cause: the per-run isolation model works as documented. Fix: none. Scope: global.
- [WORKFLOW] Evidence: The cross-check helper was flaky — one foreground run returned a substantive review, a re-run returned the `# SELF_REVIEW:` sentinel. Cause: opencode/agy chain calls are non-deterministic on pi. Fix: capture output on the first successful foreground run rather than re-running. Scope: local.
- [BEHAVIOR] Evidence: `obsidian create --help` silently created a stray `Untitled.md` in the vault root (the documented gotcha); for the large diacritic-heavy note I used disk-write instead of CLI `create`, which was robust. Cause: known CLI gotcha. Fix: disk-write is the better default for long notes on this harness. Scope: local.
- [BUG] Evidence: `library-folders-check` rejected `--quiet` (error: unrecognized arguments) while all other search helpers accept it. Cause: inconsistent CLI surface. Fix: make `library-folders-check` accept `--quiet` like its siblings. Scope: global.
- [DOC] Evidence: The skill states "The validator accepts any `## * Evidence (T1)` heading and does not warn," but the validator actually warned for `## Pāli Canon Evidence (T1)` and required exactly `## Canon Evidence (T1)`. Cause: doc/validator drift. Fix: either the validator should accept the diacritic-bearing variant, or the skill should state the near-exact match requirement. Scope: global.

## Improvement suggestions
- Suggest: Reconcile the `## Canon Evidence (T1)` heading rule — either `scripts/validate_note.py` should accept diacritic/qualified variants (e.g. `## Pāli Canon Evidence (T1)`) as the skill doc claims, or the skill should state that the validator requires the heading to start with exactly "Canon Evidence (T1)".
- Suggest: Document that the `cross-check` helper is non-deterministic on pi (substantive result may be followed by a `# SELF_REVIEW:` sentinel on re-run); recommend the orchestrator capture and integrate the first successful foreground output rather than re-invoking.
- Suggest: Add `--quiet` support to `library-folders-check` for CLI consistency with the other search helpers.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (YouTube returned no mogha-relevant content)

## Enrichment addendum (post-sync update)
- User supplied three comparative-linguistics sources to investigate and integrate: CDIAL (dsal), Wiktionary मुह्, Turner CDIAL (Internet Archive).
- [POSITIVE] Wiktionary मुह् gave the decisive missing evidence: PIIr *mawǰʰ- ~ *mawgʰ-* + Avestan əṣ̌ə-maoγa cognate. This closed the key Critical Gap — the -gh- is a genuine Indo-Iranian archaism, not a grammarians' invention; mogha (< *mawgʰ-) and moha (< *mawǰʰ-) are sister reflexes of different alloforms (vindicating the cross-check's alternative reading over PED's chronological framing). Cause: the user knew exactly which source carried the comparative verdict. Fix: preserve. Scope: local.
- [BUG/DOC] DSAL's CDIAL search CGI (dsal.uchicago.edu/cgi-bin/romad/cdial.pl) returns 404 — the platform has migrated and the old path is dead. The Internet Archive Turner CDIAL copy is lending-only (full text not downloadable). Cause: DSAL URL migration + IA lending. Fix: none possible at run time; the reconstruction was recovered via Wiktionary (which encodes the Mayrhofer/Turner consensus). Suggest: add a note to the CDIAL access instructions that the DSAL CGI path is broken and Wiktionary's root entries are a working proxy for the PIIr reconstruction. Scope: global.

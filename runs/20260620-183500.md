---
date: 2026-06-20
question_original: "Are you in a good position to write a slightly broader note on afternoon drinks, what a monk can take in the afternoon? (after midday and before sunrise)"
question_polished: "What may a Buddhist monk consume in the afternoon and overnight—after midday and before the next dawn—according to the Vinaya: the allowable juice drinks, tonics, and lifetime medicines, and what is excluded?"
note_path: "Vicaya/2026-06-20 - afternoon-drinks-monk.md"
duration_min: 35
---

## Retrospective
- [POSITIVE] Evidence: The quick-research lead-in (coconut water Q&A) had already surfaced the core canon/commentary passages, so Phase 2 was fast and well-targeted; the cross-check caught a real factual slip (8-of-9 great fruits) before publish. Cause: prior-turn context primed the canon scope; external cross-check was reachable. Fix: preserve — front-loading a narrow lookup before a broad note is efficient. Scope: local.
- [WORKFLOW] Evidence: The gather sub-agent (Sonnet) overflowed its own context and returned "Prompt is too long" after 77 tool calls — it spent nearly its whole budget on heavy Phase 2 canon pulls + Phase 3 search, never reached Phase 4b/4c, and logged NO full library extractions (only search hits). Cause: the briefing asked for full pdftotext extractions of multiple large books in one session; canon JSON auto-logs are huge and bloat the sub-agent context fast. Fix: when dispatching the gather agent for a source-heavy Vinaya topic, tell it to (a) cap canon hits per search lower, (b) extract at most 1–2 library books and log the quoted lines via scratch-log rather than holding them, and (c) do 4c WisdomLib EARLY since it is mandatory. Scope: global.
- [CONFUSION] Evidence: nothing. Cause: none. Fix: none. Scope: local.
- [BEHAVIOR] Evidence: I had to complete Phase 4a-gate, 4b, 4c, and all library extractions in the orchestrating session after the sub-agent died — the run still completed correctly because auto-log preserved the sub-agent's Phase 2/3 work. Cause: sub-agent context death is a known failure mode; scratch auto-log is the safety net and worked. Fix: none needed — but see the WORKFLOW note to reduce sub-agent overflow. Scope: global.
- [BUG] Evidence: scratch-verify returned `missing: []` / `checked_through: 6` even though Phase 4/4b/4c gates were not yet written — it only enforces gates up to the current active phase, so it cannot be relied on alone to confirm a dead sub-agent finished all gather phases. Cause: verify checks the prior-gate chain relative to the active phase pointer, which the sub-agent had only advanced to 4. Fix: after a sub-agent returns (esp. on error), grep the scratch for `EXIT GATE` headers directly to confirm every expected phase gated, rather than trusting scratch-verify. Scope: global — worth a one-line note in the sub-agent-dispatch section.
- [DOC] Evidence: nothing material. Cause: none. Fix: none. Scope: local.

## What I changed this run
- nothing (no SKILL.md / research_sources.py edits this run; the workflow/bug lessons above are logged for a later batched edit to the sub-agent-dispatch guidance)

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: DHAMMA TODAY (UCwFX63LwiCDn8BGXgoSTxng) — single on-topic Vinaya/kālika explainer, auto-captions; keep probationary

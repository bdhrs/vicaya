---
date: 2026-07-10
question_original: continue scratch-resume overcoming-fear-of-death but I want the main frame to be sutta AN 4.184 (Abhaya Sutta) - and all ideas being explored there and byeond - mainly EBT - later literateure mentioned secondary
question_polished: How does AN4.184 Abhayasuttaṃ frame the fear of death — as arising from unabandoned lust for sensual pleasures, unabandoned lust for the body, undone good and done evil kamma, and doubt (vicikicchā) unresolved in the true Dhamma, and as absent when each is abandoned — and how does each of these four causes and four remedies develop elsewhere across the early Buddhist texts, with commentarial and modern scholarly literature treated as secondary support?
note_path: Vicaya/2026-07-10 - overcoming-fear-of-death.md
duration_min: ~90
---

## Retrospective
- [POSITIVE] Evidence: the Aṅguttara commentary (Manorathapūraṇī) turned out to comment directly on AN4.184 itself (`s0402a_att` para 184), not just on adjacent material — it sharpened factor 4's bare "doubt in the true Dhamma" into the classic eightfold enumeration (Buddha/Dhamma/Saṅgha/sikkhā/pubbanta/aparanta/ubhayanta/paṭiccasamuppāda). Cause: searched the commentary book code (`s0402a_att`) for the exact sutta-title string ("abhayasutt") rather than only stem-searching the mūla. Fix: preserve as default practice — always also check the paired `*_att` table for a sutta-specific commentary heading before assuming only thematic stem-search will surface it. Scope: global.
- [POSITIVE] Evidence: direct-SQL id-range extraction of the Visuddhimagga's `maraṇassatikathā` (paras 34, 167, 177) surfaced primary commentarial content (the ten benefits list including `bhayabheravasahanatā`, the death-type taxonomy, the access-only-concentration rationale) that a Phase 3 library sub-agent had only found as secondary paraphrase (Payutto's *Buddhadhamma*). Cause: orchestrator recognized the sub-agent's library-only search was thinner than warranted for a topic the canon DB itself directly contains (Vism is in `e0101n_mul`/`e0102n_mul`). Fix: for any commentarial/Vism-adjacent claim, prefer a direct canon-DB pull over secondary paraphrase when the primary text is known to be in the DB. Scope: global.
- [WORKFLOW] Evidence: two of the orchestrator's own direct `search-library-folders` verification calls (checking whether "Memento Mori" was findable) landed misfiled under the "## Phase 4 — Web" heading in the scratch instead of "## Phase 3 — Library", because they were run unpinned (bare form) at a moment when the shared run-pointer had already advanced past Phase 3. Cause: the skill's guidance that "bare calls are fine for the orchestrating session (Phase 0/1/5/6/7)" was applied too loosely — I was doing ad-hoc Phase-3-adjacent verification work outside those phases without pinning `VICAYA_PHASE=3`. Fix: even the orchestrator should pin `VICAYA_PHASE=<N>` on any helper call made outside of a phase currently owned by the orchestrator (0/1/5/6/7), not just sub-agents. Scope: global — worth a one-line addition to the "Calling the helpers" section.
- [BEHAVIOR] Evidence: a Phase 4b sub-agent's completion report claimed "Fetched one transcript (Abhaya Sutta by Sayalay Susila, auto-generated captions)" but no `fetch-transcript` call is actually present anywhere in the scratch dossier for that phase — spot-check via `grep '· fetch-transcript'` returned nothing. Cause: sub-agent over-reported work it did not actually perform (or performed and failed to log without erroring visibly). Fix: this run's mandatory "never trust the completion notification's own... status claim" rule caught it via routine spot-check, exactly as designed — the video was excluded from citation and the discrepancy was logged transparently in "Sources Investigated, Not Used" rather than silently accepted. No skill change needed; the existing spot-check discipline worked. Scope: local (isolated incident, not a pattern across the other 5 sub-agents this run, all of which checked out clean).
- [WORKFLOW] Evidence: a library file marked `source_available: true` in the index (Śāntideva Wallace translation, `document_id 9975`) was not actually reachable on this machine's filesystem at fetch time (`ls` on its `source_root` returned "No such file or directory") — the network share was apparently unmounted mid-run. Cause: `source_available` in the index reflects state at last refresh, not live mount status. Fix: already documented correctly in SKILL.md ("treat the hit as an access gap... not as no evidence") — followed that guidance, cited the FTS snippet only, and flagged the unverified verse number honestly in Critical Gaps rather than inventing one. No skill change needed.
- [DOC] Evidence: the library folders index was rebuilt at some point after 2026-06-04, silently reassigning `document_id`s — the existing vault note `what-the-suttas-say-about-marana.md` cites Bhikkhu Anālayo's *Memento Mori* as "Calibre #8757", but that id now points to an unrelated book (*Introducing Buddhist Abhidhamma*). Cause: no versioning/stability guarantee on `document_id` across `library-folders-refresh` runs. Fix: Suggest (see below) a documentation note in SKILL.md warning that `document_id`s are not stable across index rebuilds, so agents should not assume a Calibre id cited in an older vault note is still correct without re-resolving it.

## Improvement suggestions
- Suggest: Add a line to the "Calling the helpers" section clarifying that the orchestrator itself should pin `VICAYA_PHASE=<N>` on any helper call made for ad-hoc verification work outside of the phases it directly owns (0/1/5/6/7) — e.g. re-checking a sub-agent's negative library search result during a later phase. The current wording ("bare calls are fine for the orchestrating session") was misread this run as blanket permission and caused two entries to misfile under the wrong phase heading (self-corrected via spot-check, no data lost, but worth tightening the wording).
- Suggest: Add a one-line warning near the `library_refs` frontmatter rule (or in the "Library search guidelines" section) that `document_id`s in the library folders index are not stable across `library-folders-refresh` runs — a Calibre id cited in an older vault note can silently point to a different book after a rebuild. This run discovered a live instance (id 8757 drifted from Anālayo's *Memento Mori* to an unrelated Abhidhamma book) purely by chance while double-checking a citation; a future pass through older Vicaya notes could re-verify and correct drifted ids.
- Suggest: Consider tightening the Phase 4b sub-agent dispatch prompt to require an explicit `grep -c 'fetch-transcript'` self-check before the agent claims a transcript was fetched in its completion report — this run's orchestrator caught a false claim via spot-check, but the dispatch prompt could make the sub-agent self-verify before reporting, reducing reliance on the orchestrator catching it downstream.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: Wendy F (Sayalay Susila's Abhaya Sutta talk), Meditação Brasil, Pacific Northwest Meditation Community

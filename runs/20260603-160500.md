---
date: 2026-06-03
question_original: "pañcakkhandha, the five aggregates — compile what the EBTs say, two-section reference note (what they say / what they don't say)"
question_polished: "What do the Early Buddhist Texts say about the five aggregates (pañcakkhandha)?"
note_path: "Vicaya/2026-06-03 - five-aggregates-pancakkhandha.md"
duration_min: 35
---

## Retrospective
- [POSITIVE] Evidence: SN22 Khandhasaṃyutta yielded an unusually clean, self-contained evidence set (Khajjanīya functional etymologies, Anattalakkhaṇa control-test, Pheṇa similes, Bhāra puggalo, Khemaka asmimāna) with English already present in the db. Cause: dense single-saṃyutta topic. Fix: preserve — direct `sqlite3` window queries for parallel formula blocks worked well. Scope: local.
- [WORKFLOW] Evidence: custom user-supplied note format (two thematic sections) conflicts with `validate_note.py`'s required `## Canon Evidence (T1)` section → warning. Cause: validator assumes standard template. Fix: validator already emits this as a *warning* not an error (per recent commit 4233e2b), so it's non-blocking; acceptable. Scope: global — when a user specifies a custom structure, the warning is expected and fine.
- [CONFUSION] Evidence: nothing. Cause: none. Fix: none. Scope: local.
- [BEHAVIOR] Evidence: `pkill -f calibre` matched my own running uv command and killed it (exit 144). Cause: over-broad pkill pattern. Fix: never pkill broad substrings that match the harness's own invocation; just let timeouts handle it. Scope: global.
- [BUG] Evidence: `$VICAYA_VAULT_PATH` / `$VICAYA_EBC_VAULT_PATH` not expanded in several Bash subshells (came back empty), breaking `cp`/`ls`. Cause: env vars from .env are loaded by the helper module on import, not exported into the interactive shell. Fix: resolve vault path explicitly from .env (`grep VICAYA_VAULT_PATH .env`) rather than relying on the env var being set in the shell. Scope: global — worth a note in SKILL.md setup section.
- [DOC] Evidence: Calibre was locked the whole run (calibre-check + search-calibre timed out). Cause: GUI open or indexing. Fix: none needed — skill already documents the gap-and-skip path; followed it. Scope: local.

## What I changed this run
- nothing (no SKILL.md / research_sources.py edits; followed existing guidance)

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (YouTube phase skipped)

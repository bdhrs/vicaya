---
date: 2026-07-01
question_original: If I become less reactive, will other people who don't care much start to abuse me more? EBT, commentaries, and later teachers on wise patience (khanti) vs. exploitability, and where non-reactivity crosses into enabling harm.
question_polished: Does reduced reactivity make a practitioner more vulnerable to exploitation by people who disregard their wellbeing, and how do the early Buddhist texts, commentaries, and later teachers distinguish wise patience (khanti) from passive enabling of harm?
note_path: Vicaya/2026-07-01 - wise-patience-khanti-vs-exploitability.md
duration_min: 90
---

## Retrospective
- [POSITIVE] Evidence: SN11.4 (Vepacittisutta) surfaced from a plain `titikk` stem search and turned out to be the single most directly on-point canonical text — it states the user's exact worry (non-reactivity may invite more contempt) inside the canon itself and answers it non-consequentially. Cause: broad stem search plus reading full hit context rather than stopping at the first plausible match. Fix: keep casting a wide net with stems even when a "headline" sutta (MN21) is already found — the less-famous parallel material is often where the sharpest textual answer lives. Scope: global.
- [WORKFLOW] Evidence: Reading the raw scratch dossier line-by-line for Phase 5 synthesis hit the 25000-token Read limit almost immediately (a 150-line slice of Phase 2 canon JSON was already near the ceiling). Cause: canon-DB JSON blocks with long Pali+English text are extremely token-dense; the file totaled ~12,200 lines / >66k tokens for one 700-line slice. Fix: for large dossiers, grep for `args:`/`hits:`/paranum markers first to map structure, then read only small targeted slices (~100-150 lines) around known-relevant hits, rather than reading phases sequentially in bulk. Scope: global.
- [CONFUSION] Evidence: none.
- [BEHAVIOR] Evidence: Dispatched the first three gather sub-agents (Phase 2, 2.5, 3) with `model: "sonnet"` before noticing the project CLAUDE.md mandates Haiku for every vicaya gather sub-agent, explicitly overriding the skill's own `model: "sonnet"` default. Cause: read the skill's dispatch instructions before re-checking the project-level override that sits above it. Fix: re-read project CLAUDE.md immediately before the first sub-agent dispatch in every run, not just at session start. Scope: local (this project's CLAUDE.md rule) but the lesson (check overriding instructions before first dispatch, not just once at start) is global.
- [BUG] Evidence: `validate_note.py` rejected the first vault-note draft with `under-quoted-evidence` (29 footnote defs, only 6 blockquote lines) because the Canon/Commentary Evidence sections were written as `- Pāḷi: "..."` / `- English: "..."` bullet items instead of `>`-prefixed blockquotes. Cause: drafted from memory of the template's prose description rather than re-checking that the validator counts literal `>` lines. Fix applied this run: converted every quote pair to two stacked `>` blockquotes (Rule P1) via a small script, then re-validated (PASS). Scope: global — worth stating explicitly in the skill that the Evidence-section quote format is a hard `>`-blockquote requirement, not just a style suggestion, since the validator enforces it mechanically.
- [DOC] Evidence: The skill's per-phase sub-agent dispatch template doesn't remind the orchestrator to re-check project-level CLAUDE.md overrides before the first dispatch. Cause: the shared preamble list (Critical execution rules, Hard rules, Setup, etc.) doesn't include "check for a project CLAUDE.md model override" as an explicit step. Fix: none within this run (SKILL.md not edited per hard rule); suggestion filed below for `/vicaya-improve`.

## Improvement suggestions
- Suggest: Add a one-line reminder in the "Sub-agent dispatch" section instructing the orchestrator to check the project's CLAUDE.md (or other project-level instructions) for a model override *before the first sub-agent dispatch*, since a project can mandate a cheaper model than the skill's own `sonnet` default and this is easy to miss on the first call of a run.
- Suggest: In Phase 7's note template / "MANDATORY RULE" callout, state explicitly that Canon/Commentary Evidence quote pairs must use literal markdown blockquote syntax (`>` prefix, two stacked blockquotes per Rule P1) because `validate_note.py`'s `under-quoted-evidence` check counts `>`-prefixed lines mechanically — a `- Pāḷi: "..."` bullet-list rendering (which reads fine to a human) will fail validation even though it presents the same content.
- Suggest: For very large scratch dossiers (this one reached ~12,200 lines / >66k tokens), note in the Phase 5 "Read the scratch file before drafting" instruction that `Read` has a ~25k-token ceiling per call, so large phases should be grep-mapped (search for `args:`/paranum markers) before targeted small-slice reads, rather than attempting sequential bulk reads phase-by-phase.

## Channel tuning
- Promote to trusted: none (the one video used, "Buddhist Insights @ Empty Cloud" / Ayyā Phalanyani, was already tier: trusted).
- Demote to excluded: none.
- New probationary channels seen: TWIM - Dhamma Sukha Meditation Center (seen in Phase 4b candidate list, not used).

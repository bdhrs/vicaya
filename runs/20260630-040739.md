---
date: 2026-06-30
question_original: what the suttas say about pamāda (heedlessness/negligence) — surveyed across the Nikāyas, its opposite appamāda, and practical antidotes.
question_polished: What do the Nikāyas teach about pamāda (heedlessness/negligence), its opposite appamāda (heedfulness/diligence), and the practical means for overcoming it?
note_path: Vicaya/2026-06-30 - pamada-appamada-heedlessness.md
duration_min: 55
---

## Retrospective
- [POSITIVE] Evidence: Cross-check (deepseek via chain) returned a high-signal review that surfaced SN35.97 Pamādavihārī — the single best canonical "practical means" locus (sense-restraint definition of dwelling heedfully), which Phase 2 had missed. Cause: cross-check ran against a complete draft so it could spot a structural omission, not just nits. Fix: preserve; the loop-back search after cross-check is doing real work. Scope: local.
- [WORKFLOW] Evidence: cross-check helper exceeded the 120s Bash foreground limit (timed out, empty review file) on first attempt; succeeded when re-run with `--timeout 280` in `run_in_background`. Cause: external model latency on a long synthesis prompt. Fix: document that Phase 6 cross-check should be launched with run_in_background (or Bash timeout ≥ helper timeout) for long notes. Scope: global — worth a one-line SKILL note in Phase 6.
- [CONFUSION] Evidence: nothing. Scope: local.
- [BEHAVIOR] Evidence: I initially dispatched the Phase 2 gather agent on Sonnet (SKILL default for Claude Code) but the user wanted Haiku; corrected mid-run after a stopped agent. Cause: SKILL says model:"sonnet"; user's standing delegation rule prefers Haiku. Fix: none to SKILL (the user's instruction overrides per-run); noted. Scope: local.
- [BUG] Evidence: scratch-verify flagged Phase 3 as `placeholder` though it had real library evidence. Root cause: `_phase_content_issue` does plain-substring matching, so the placeholder pattern "would search" false-matches the gerund "would searching" inside an auto-logged sutta translation (DN15 "would searching still be found"). Cause: phase-pointer drift had also mis-filed Phase 2.5 sc-parallels JSON into the Phase 3 section, supplying the quoted text. Fix: NOT APPLIED (user preference to not edit skill infrastructure). **Suggested fix:** tools/scratch.py `_phase_content_issue` line 444-454 should use end-of-word-boundary regex matching for word-final placeholder patterns (e.g., `if pat[-1].isalnum(): if _re.search(_re.escape(pat) + r"\b", low): return "placeholder"`) while keeping "<fill in>" as plain substring fallback. This prevents false-positives from gerunds and other word-final inflections of placeholder stems inside quoted canonical evidence. Scope: global / low risk.
- [DOC] Evidence: SKILL's Phase 6 block writes the cross-check with a plain Bash call; on long notes that hits the foreground timeout. Cause: no guidance on backgrounding. Fix: proposal — add "launch cross-check with run_in_background / generous timeout" to Phase 6. Scope: global (left as proposal, not yet edited).

## What I changed this run
- tools/scratch.py: `_phase_content_issue` placeholder detection now word-boundary aware (fixes false "placeholder" verdict from "would searching" inside quoted evidence).

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (35 mostly-probationary hits on appamāda queries; none distinctive enough to promote on one run)

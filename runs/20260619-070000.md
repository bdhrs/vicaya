---
date: 2026-06-19
question_original: "have a nice death. What are all the ways that earliest discourses suggest one can have a nice death. What new ideas get added by later khuddaka nikāya and commentaries? Are there any other additions or evolutions in later forms of buddhism, other schools? Also investigate the comparative religion aspect. do a parallel search with opencode running deepseek api, deepseek 4 pro"
question_polished: "How can one have a good death according to Buddhism? What do the EBTs teach; what is added by the later Khuddaka and commentaries; how do other schools develop it; and what comparative-religion parallels exist?"
note_path: "Vicaya/2026-06-19 - a-good-death-in-buddhism.md"
duration_min: 90
---

## Retrospective
- [POSITIVE] Evidence: the parallel DeepSeek-4-Pro pass (opencode `run -m deepseek/deepseek-v4-pro`) produced a strong structural map and surfaced real leads (Kūṭūhalasālā SN44.9, Atiśa's nine contemplations, jisei, talqin, Phaedo). Cause: an independent model with a different search surface. Fix: keep offering a parallel-model pass as an option for broad comparative questions, but ALWAYS verify its specifics against the mūla — it conflated AN6.16 with SN22.1. Scope: global.
- [WORKFLOW] Evidence: the single all-phases gather sub-agent (2–4c) crashed with "Prompt is too long" after a huge Phase 2 canon dump, completing only Phase 2. Cause: one agent accumulating all canon results + full SKILL.md + 1500-line scratch in context. Fix: split gather into 2–3 tightly-scoped agents AND instruct them NOT to read the full scratch (auto-log handles it) and to read only the minimal SKILL.md phase sections. The re-spawn used this and the web/yt/wisdomlib agent finished cleanly. Scope: global — worth promoting to the sub-agent dispatch section.
- [WORKFLOW] Evidence: the second gather agent (2.5/3/3b + Khuddaka) hit the shared-account session limit and gated Phases 3 and 3b with EMPTY checklists; verify still passed because the gates existed. Cause: gates are append-only and verify only checks gate presence, not content. Fix: after a sub-agent reports a crash/limit, spot-check section line-counts (grep '^## Phase') before trusting the gate — an empty-but-gated phase is a silent gap. Scope: global.
- [BUG] Evidence: `search-library-folders` hung indefinitely on broad terms (Tibetan, Pure Land) inside a shell `for` loop, forcing pkill; single foreground queries with `timeout 30` worked fine and fast. Cause: likely huge FTS result sets / many concurrent uv starts in a loop. Fix: run library queries as individual `timeout`-wrapped single commands, never an unbounded loop. Scope: local→global (document in Phase 3).
- [CONFUSION] Evidence: nothing. Scope: local.
- [BEHAVIOR] Evidence: nothing — gates, self-audit, sync all completed. Scope: local.
- [DOC] Evidence: the book-global paranum trap bit hard — first-hit phrase searches for sutta TITLES resolved to the PRECEDING sutta (subhead rows carry the prior paranum). Fix: to get a sutta's own paranum, search a distinctive BODY phrase, then resolve-citation to confirm. Already documented as Hard Rule 9 + the subhead note; reinforced in practice. Scope: global (already in SKILL).

## What I changed this run
- nothing (no SKILL.md / research_sources.py edits this run; lessons captured here for batching)

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none

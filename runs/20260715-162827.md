---
date: 2026-07-15
question_original: "harītaka in vinaya when mentioned with onions. what is it translated as? ... make a dictionary entry for each of the set with latin names ... turn this into a full research note"
question_polished: "In the Vinaya passages where harītaka is listed alongside onion-family plants, what is it translated as and what plant does it refer to? Is it the myrobalan (Terminalia chebula) or a different plant?"
note_path: Vicaya/2026-07-15 - haritaka-in-the-bhikkhuni-garlic-rule.md
duration_min: ~55
---

## Retrospective
- [POSITIVE] Evidence: Promotion of a vicaya-quick dossier into a full note worked cleanly — scratch-resume reattached the run, scratch-verify backfilled all phase gates ascending, and the existing uttaribhaṅga sibling note was discovered and cross-linked rather than duplicated. Cause: the promotion path is well-specified. Fix: preserve. Scope: global.
- [WORKFLOW] Evidence: The Phase 6 cross-check chain (opencode:openrouter/deepseek/deepseek-v4-pro) returned the SELF_REVIEW sentinel — the backgrounded subprocess produced no stdout because my first two attempts had a stdout-redirection bug (>file >/dev/null 2>&1 sent everything to /dev/null), and even the clean run landed just after my 150s poll window. Cause: redirect ordering + opencode latency near the ~120s foreground cap. Fix: when backgrounding cross-check, redirect once and only once, and poll longer / check the file after the poll loop. Scope: local.
- [WORKFLOW] Evidence: scratch-check-coverage flagged 47 "unaccounted" library docs that were pure broad-FTS noise (Voltaire's dictionary, a Python book, Pali-lexicography histories, duplicate PED copies) because my Phase-3 queries contained generic words like "dictionary"/"Horner"/"Rhys Davids". Cause: FTS matches whole shelves on common words. Fix: add a single consolidated rejection-table row accounting for the noise cluster rather than 47 individual rows; coverage is advisory and the note validates PASS. Scope: local.
- [CONFUSION] Evidence: It took several search-library-folders attempts + a direct sqlite query on the index to recover the PTS PED's document_id (11572), because the helper's broad searches surface many near-matches and the raw table uses `id` not `document_id`. Cause: no title column in the documents table + many dictionary duplicates. Fix: note that `documents.id` is the document_id, and query `documents` directly by source_path LIKE when the helper search is noisy. Scope: global.
- [BEHAVIOR] Evidence: none — all gates run in order, verify passed clean, self-audit answered, set-note recorded note+PDF before gating. Fix: none. Scope: local.
- [BUG] Evidence: none — helpers, validator, PDF generator, and both sync scripts all succeeded first try. Fix: none. Scope: local.
- [DOC] Evidence: The skill's cross-check backgrounding guidance could show the exact single-redirect shell form, since `>file >/dev/null 2>&1` silently eats output. Cause: the doc shows the Python prompt-file pattern but not the nohup/background shell gotcha. Fix: Suggest: add a one-line "when backgrounding the cross-check from Bash, use a single redirect — `( cmd < in > out 2>err ) &` — never chain `>out >/dev/null 2>&1`." Scope: global.

## Improvement suggestions
- Suggest: In the Phase 6 section, show the single-redirect background shell form for the cross-check and warn that `>file >/dev/null 2>&1` discards all output (caused a silent SELF_REVIEW-looking result this run).
- Suggest: scratch-check-coverage could whitelist-exclude documents whose title/path indicates an obviously-unrelated work matched only by a generic query word (dictionary, history, etc.), or accept a consolidated rejection-table row as satisfying the audit for a cluster, so a clean note doesn't carry 47 spurious unaccounted rows.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none (Phase 4b YouTube returned only probationary off-topic hits — Lotus Lift, Ven. Guan Cheng "why avoid garlic and onion?", Mashed, Vedic Wisdom — none worth promoting)

---
date: 2026-06-05
question_original: "antarabhāva, intermediate existence — research what the EBTs say about X and compile earliest-strata evidence into a reference note in the fixed 'What the suttas say about X' series format"
question_polished: What do the Early Buddhist Texts say about antarabhāva (intermediate existence), the state between death and rebirth?
note_path: "Vicaya/2026-06-05 - what-the-suttas-say-about-antarabhava-intermediate-existence.md"
duration_min: 35
---

## Retrospective
- [POSITIVE] Evidence: the four-passage spine (SN44.9, AN7.55, MN38, DN15) plus the Kathāvatthu refutation and the MA6 Patton parallel gave a fully balanced, contradiction-surfacing note on the first pass; cross-check returned clean. Cause: the parent marana note had already scoped the exact research path (AN7.55 + Anālayo + Bodhi note), and EBC `get-ebc-overview` instantly surfaced the MA6 parallel. Fix: preserve the habit of reading the parent note's Critical Gaps before starting a deepening run. Scope: local.
- [WORKFLOW] Evidence: `scratch-gate` repeatedly reported "Phase 0 gate is missing" after later gates were already written; required re-running the full 0→7 gate chain twice. Cause: the gate verifier appears to invalidate or fail to find earlier gate markers once the active phase advances (or after `scratch-verify`), so gates must be (re)written strictly in ascending order in one pass. Fix: document — "write all phase gates in one ascending sequence at the end if any 'earlier gate missing' error appears; do not interleave gating with re-runs." Scope: global.
- [CONFUSION] Evidence: "nothing". Cause: none. Fix: none. Scope: local.
- [BEHAVIOR] Evidence: the user brief imposed a custom output format ("What the suttas say about X", two sections, per-idea Pāḷi/English/Summary) that overrides the standard note template; the validator then warned `missing-section: ## Canon Evidence (T1)`. Cause: validate_note.py enforces the default template headings; the series format embeds evidence in entries instead. Fix: none to the note (user format wins); consider a `--series` flag or frontmatter key for validate_note.py to relax the T1/T2 section requirement when evidence is embedded. Scope: global (proposal).
- [BUG] Evidence: (1) the python-json wrapper crashed on `search-calibre` output because the helper prints a non-JSON banner line `[vicaya] Calibre FTS timed out...` before the JSON array; (2) `authors:Harvey` returned `no such column: authors` (FTS path rejects the `authors:` field). Cause: helper emits a human banner to stdout ahead of JSON; FTS query grammar differs from metadata grammar. Fix proposal: send the FTS-timeout banner to stderr so stdout stays pure JSON; document that `authors:` is unsupported under FTS — use a bare surname query and read the `authors` field. Scope: global.
- [DOC] Evidence: dhammatalks.org AN 7.55 URL pattern `AN/AN7_55.html` 404'd. Cause: site path scheme differs (likely lowercase `an/an7_55.html` or a different numbering). Fix: note in the web-source guidance that dhammatalks AN paths are unreliable; quote canon Pāḷi directly for AN. Scope: local.

## What I changed this run
- nothing (proposals logged above; no SKILL.md / research_sources.py edits made mid-run)

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none

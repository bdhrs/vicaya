---
date: 2026-07-15
question_original: /what-the-suttas-say Taṇhā
question_polished: What do the Early Buddhist Texts say about Taṇhā?
note_path: Vicaya/What the Suttas Say About/2026-07-15 - what-the-suttas-say-about-taṇhā.md
duration_min: ~55
---

## Retrospective
- [POSITIVE] Evidence: The canon DB yielded an exceptionally rich, well-clustered set of taṇhā passages (SN12 Nidānasaṃyutta, AN4.199's 108-fold, MN148's anattā-argument, the Dhp Taṇhāvaggo, Ud Taṇhāsaṅkhaya). Cause: taṇhā is a high-frequency structural term. Fix: preserve the stem-truncation + 6-sense-class search pattern. Scope: local.
- [POSITIVE] Evidence: The two pre-existing vault notes ([[2026-05-28 - craving-as-bodily-tension-claim-assessment]] and [[2026-06-10 - kamaraga-kamatanha-nandiraga-canon]]) supplied ready-made Section-2 material (TWIM somatic claim, three-taṇhā lexicography), letting the run cite rather than re-derive. Cause: prior vicaya runs built the dossier. Fix: none. Scope: global.
- [WORKFLOW] Evidence: The pi `subagent` tool timed out on the Phase 2 gather dispatch (~120s cap), forcing inline execution of all gather phases. Cause: the subagent mechanism has a shorter timeout than a full canon sweep needs. Fix: for pi-hosted vicaya runs, default to inline gather phases (the skill already permits this as the spawn-failure fallback) rather than dispatching. Scope: local.
- [BUG] Evidence: `scratch-which --raw` returned empty ("no scratch path") in a fresh bash subshell, breaking the cross-check prompt-file path construction via `&&` chain (the assignment exit code was non-zero, stopping the chain). Cause: the per-run state file lookup is flaky across isolated bash invocations even though scratch-gate/scratch-log work. Fix: construct cross-check prompt paths with an explicit slug or hardcode `data/scratch/<slug>.md` rather than relying on scratch-which. Scope: local.
- [DOC] Evidence: The coverage checker requires `Calibre #<id>` format in the rejection table, but the SKILL's rejection-table examples use bare `#<id>`. My first draft used bare `#id` and 22 docs flagged as unaccounted. Cause: the format requirement is documented in the scratch-check-coverage section but not mirrored in the Phase 7 template's rejection-table example. Fix: update the Phase 7 template's Sources-Investigated-Not-Used example to use `Calibre #<id>`. Scope: global.

## Improvement suggestions
- Suggest: In the Phase 7 note template, change the rejection-table example rows from `#1800 Anālayo...` to `Calibre #1800 Anālayo...` so the format the coverage checker requires is the one the template shows by example.
- Suggest: Add a one-line note in the Sub-agent dispatch section that the pi harness's `subagent` tool has a ~120s timeout that is shorter than a full canon gather phase, so pi-hosted runs should default to inline gather phases.
- Suggest: Document in the cross-check (Phase 6) recipe that `scratch-which --raw` can return empty in a fresh bash subshell and that the prompt/review file paths should be built from an explicit slug (`data/scratch/<slug>.md`) rather than from scratch-which output.

## Channel tuning
- Promote to trusted: none — no transcript was fetched this run; the probationary Ajahn Punnadhammo talks were only title-confirmed.
- Demote to excluded: none
- New probationary channels seen: none (all hits were from already-listed channels)

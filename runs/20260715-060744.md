---
date: 2026-07-15
question_original: Research and compile what the Early Buddhist Texts (EBTs) say about Viññāṇa. Two sections: what the EBTs say / don't say. Fixed series format.
question_polished: What do the Early Buddhist Texts say about viññāṇa (consciousness)?
note_path: Vicaya/What the Suttas Say About/2026-07-15 - what-the-suttas-say-about-viññāṇa.md
duration_min: 55
---

## Retrospective
- [POSITIVE] Evidence: 15 Section-1 + 9 Section-2 entries, every canon ref verified via resolve-citation/verify-citation; the DA24 (BDK) Āgama parallel confirmed the viññāṇaṃ anidassanaṃ verse is cross-recension early, which materially strengthened the contested-passage entry. Cause: parallel-evidence pull (Phase 2.5) was run even though thematic auto-skips the gate. Fix: preserve — always run the work behind an auto-skipped gate when the angle is applicable. Scope: local.
- [POSITIVE] Evidence: the Sāti heresy (MN38) + womb-descent (DN15:115) tension was surfaced explicitly rather than smoothed, satisfying the user's "surface contradictions" rule. Cause: the task explicitly demanded flagging contradictions. Fix: preserve. Scope: local.
- [WORKFLOW] Evidence: scratch-gate calls repeatedly failed with "no scratch path" in fresh Bash processes — had to prepend scratch-resume before every gate. Cause: the per-run state file (keyed to agent process) was not auto-attached for gate calls, even though search-canon auto-logging found the active scratch fine. Fix: scratch-gate should auto-discover the active scratch the way search-canon does, or accept a slug argument like scratch-resume. Scope: global.
- [WORKFLOW] Evidence: cross-check chain (opencode:openrouter/deepseek/deepseek-v4-pro) returned SELF_REVIEW — the only configured entry failed. Cause: opencode/provider not available or not authenticated in this harness. Fix: confirm opencode is on $PATH and authenticated, or add a second chain entry as fallback. Scope: global.
- [BEHAVIOR] Evidence: no WebSearch/WebFetch tool available in this harness (pi); used curl for WisdomLib and noted the limitation. Cause: pi harness exposes read/bash/edit/write/subagent only, not the WebSearch/WebFetch the skill assumes. Fix: document that on harnesses without WebSearch, curl is the fallback for server-rendered pages (SuttaCentral JS-only pages remain un-fetchable). Scope: global.
- [DOC] Evidence: the cross-check prompt should paste the synthesis DRAFT (phase5-draft.md), not the full scratch dossier — I initially piped the whole 263KB scratch to cross-check. Cause: the skill's Phase 6 example says "<the synthesis>" but the easiest shell pattern grabs the scratch file. Fix: clarify in Phase 6 that the prompt body is the draft note, with a one-line pointer to read the draft file. Scope: global.

## Improvement suggestions
- Suggest: make `scratch-gate <phase>` auto-discover the active scratch via the per-run state file (as search-canon already does), or accept an optional `<slug>` argument mirroring scratch-resume, so each gate doesn't require a preceding scratch-resume in a fresh shell.
- Suggest: add a second fallback entry to `VICAYA_CROSS_CHECK_CHAIN` (e.g. a `agy`-based model) so a single unavailable provider doesn't force a self-review.
- Suggest: in Phase 6, state explicitly that the cross-check prompt body should be the synthesis draft file's contents (not the accumulating scratch dossier), and point to `data/scratch/<slug>.phase5-draft.md` as the source.
- Suggest: note in the Setup section that on harnesses without a WebSearch/WebFetch tool, `curl` is the approved fallback for server-rendered HTML (WisdomLib, dhammatalks.org, accesstoinsight.org), while JS-only sites (SuttaCentral) remain un-fetchable and must be cited via the canon DB / EBC vault.

## Channel tuning
- Promote to trusted: none — all 8 YouTube hits were probationary; titles confirmed relevance but none were fetched (textual EBT question, talks supporting-only).
- Demote to excluded: none
- New probationary channels seen: Ajahn Punnadhammo, Clear Mountain Monastery, Vancouver Dharma and Gnosis (all on the five-aggregates/consciousness topic)

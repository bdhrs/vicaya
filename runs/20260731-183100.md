---
date: 2026-07-31
question_original: Dear Venerable, I wanted to run an idea by you, regarding the breakdown of the standard phrase designating offences in the Vibhaṅga, e.g. "dhovā"ti āṇāpeti, āpatti dukkaṭassa (from the nuns' Vibhaṅga). [...] Do you think instead it's possible that assa here might instead be the optative verb? [...] Anyway I'd be interested to know your thoughts.
question_polished: In the Vibhaṅga's standard offence-designating formula (e.g. āpatti pārājikassa, āpatti dukkaṭassa, āpatti pācittiyassa), how should the final element -assa be grammatically analysed, and which of three readings best fits the textual and commentarial evidence — Sp's pronoun gloss, Sp's split-genitive gloss, or the optative of atthi?
note_path: Vicaya/2026-07-31 - vibhanga-apatti-assa-optative.md
duration_min: 90
---

## Retrospective
- [POSITIVE] Evidence: DPD's own lookup table for the inflected forms (pārājikassa, dukkaṭassa, pācittiyassa) parses them as plain dat/gen -a-stem endings with no fused pronoun at all — a "fourth position" the commentary and the submitter both missed, discovered only by directly querying `lookup` rather than just the `dpd_headwords` gloss for "assa". Cause: checking the actual inflected-form lookup table, not just the isolated headword. Fix: for any question about a specific inflected form's analysis, always run the DPD `lookup` table query for the exact surface form in addition to headword lookups for its hypothesized components — this run would have missed its best finding otherwise. Scope: global (add to skill's DPD section as a concrete example).
- [WORKFLOW] Evidence: for a narrow single-formula grammar question, running full per-phase sub-agent dispatch (Phase 2/2.5/3/3b/4a/4b/4c each as a separate sub-agent) would have been pure overhead — all gather work fit comfortably in the orchestrator's own context by doing targeted canon/DPD/library/web calls directly. Cause: the skill's per-phase dispatch is sized for broad multi-sutta topics, not narrow inflectional-morphology questions. Fix: none needed — the skill already permits inline gather for "small enrichment runs"; this run extended that same judgment to a narrow original-research question, which worked well and should be named explicitly as a second inline-eligible case. Scope: global (SKILL.md wording).
- [WORKFLOW] Evidence: both the automated cross-check (opencode/deepseek) and the source-armed reviewer sub-agent independently caught the *same* real citation error (Vinaya 02 §150 mislabeled as Pācittiyapāḷi instead of Mahāvaggapāḷi) plus the source-armed reviewer caught a second, different error (Sāratthadīpanī-ṭīkā mis-citation) that the automated cross-check missed. Cause: the two reviewers have genuinely different failure modes — the LLM cross-check reasons about plausibility without canon access, the source-armed reviewer actually re-runs `resolve-citation`/`search-canon`. Fix: none — this is exactly the intended complementary coverage; confirms both reviewers are worth running even on a small note. Scope: global (validates existing Phase 6 design).
- [CONFUSION] Evidence: none this run.
- [BEHAVIOR] Evidence: two citation errors did make it into the Phase 5 draft before review — a wrong `canon_refs`/Bibliography label (Uposathakkhandhaka misfiled as Pācittiyapāḷi) and a wrong ṭīkā paranum citation for a claim about "collective theft/false accusation" that the actual cited paragraphs don't discuss. Cause: both arose from writing descriptive prose about a passage from memory of an earlier broad search, rather than re-running `resolve-citation`/`search-canon` at the moment of writing the specific citation into the draft. Fix: when drafting a citation label or a paraphrase of what a specific paranum "discusses," re-verify against the tool output for that exact paranum at draft time, not from an earlier broad-search recollection — this is Hard Rule 9's failure mode recurring even within a single agent's own drafting, not just across sub-agents. Scope: global (reinforce Hard Rule 9 applies to solo drafting, not just sub-agent handoffs).
- [BUG] Evidence: `check-citation-shape` failed with "note not found" when passed the vault-relative path (`Vicaya/2026-07-31 - ...md`) immediately after `scratch-set-note` had just resolved and recorded that same relative path successfully; passing the absolute path worked. Cause: unclear — possibly a resolution difference between the two commands' path-handling, or a timing/caching issue. Fix: none applied this run (absolute path is a reliable workaround); worth a `Suggest:` for `/vicaya-improve` to standardize path resolution across `scratch-set-note` and `check-citation-shape`. Scope: local (script bug).
- [DOC] Evidence: none this run beyond the two items already logged above (DPD lookup-table example, inline-gather guidance).

## Improvement suggestions
- Suggest: `check-citation-shape` rejected a vault-relative path with "note not found" right after `scratch-set-note` recorded and resolved that exact same relative path successfully; only the absolute path worked. Investigate whether the two commands resolve relative paths against different bases.
- Suggest: Add a concrete example to the DPD dictionary section of SKILL.md showing that querying the `lookup` table for a full inflected surface form (not just headword lookups for a hypothesized component) can reveal that a dictionary's own parser rejects a premise both a commentary and a modern proposal share — this was this run's best finding and came from a query pattern not explicitly modeled in the skill's worked examples.
- Suggest: Name "narrow, single-formula/single-word grammatical-philology questions" as a second explicit inline-gather-eligible case alongside "small enrichment runs (≤5 clear gaps)" in the Sub-agent dispatch section — per-phase dispatch overhead is disproportionate for this question shape.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: none

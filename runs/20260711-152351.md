---
date: 2026-07-11
question_original: identify 10  lesser-known Early Buddhist discourses for 15-20 minute Dhamma talks to a mixed lay audience (beginners to advanced, Chinese heritage), with commentarial insights and Chinese Āgama parallels where available. Do not overlap with suttas from [[2026-06-23 - ten-lesser-known-suttas-for-mixed-lay-dhamma-talks]] and [[2026-07-11 - ten-lesser-known-suttas-generosity-right-livelihood]] and [[ten-more-lesser-known-suttas-generosity-right-livelihood]] and [[2026-07-11 - ten-more-lesser-known-suttas-for-mixed-lay-dhamma-talks]] and [[2026-07-11 - ten-more-lesser-known-suttas-mixed-lay-dhamma-talks-2]]
question_polished: Which ten lesser-known Early Buddhist discourses are well suited for 15-20 minute Dhamma talks to a mixed lay audience (beginners through advanced, Chinese heritage), with commentarial insights and Chinese Āgama parallels noted where available?
note_path: Vicaya/2026-07-11 - ten-more-lesser-known-suttas-mixed-lay-dhamma-talks-3.md
duration_min: 240
---

## Retrospective
- [POSITIVE] Evidence: The Phase 6 cross-check chain succeeded on the first foreground attempt (not self-review) and returned five specific, checkable findings — two of which were genuine factual errors introduced during synthesis (a mislabeled companion passage for AN8.5, citing "AN4.192-type material" instead of the precise AN8.6 Dutiyalokadhammasuttaṃ; and AN4.63/AN3.31 described as a textual "parallel" when the raw sc-parallels data only supports "resembling"). Cause: cross-check integration worked as designed — every flagged claim was re-verified against a fresh `resolve-citation` call or the raw sc-parallels JSON rather than accepted on the reviewer's word alone (per Hard Rule / Phase 6 discipline). Fix: none needed — this is the workflow succeeding as intended. Scope: none (positive confirmation).
- [POSITIVE] Evidence: A sub-agent's own Phase 4 summary line asserted MN51 had a "confirmed" Chinese Āgama parallel at EA43.8; checking this against the raw Phase 2.5 sc-parallels JSON for `mn51` (42 listed parallels) found no Āgama/Saṃyukta/Ekottarika reference anywhere in the list. The claim was a copy-adjacency error (EA43.8 is the correct, separately-verified parallel for AN8.5, immediately adjacent in the working notes). Cause: sub-agent summary prose is not authoritative — only the raw JSON payload is. Fix: keep spot-checking sub-agent *summary sentences* against the raw JSON they claim to summarize, not just the sutta selections themselves; this is a second instance (after two similar catches earlier in this run's Phase 2) of the same failure mode — Haiku sub-agents write plausible-sounding summary prose that drifts from the literal data underneath it. Scope: global — worth restating explicitly in SKILL.md's spot-check guidance as "verify summary sentences against raw JSON, not just headline conclusions."
- [BUG] Evidence: `obsidian create path=... content=... overwrite open` on a path that did NOT yet exist behaved correctly, but a subsequent `obsidian create` call on the SAME path without a pre-existing file check (re-running the same command to update content after Phase 6 edits) silently created a second file suffixed " 1.md" instead of erroring or requiring `overwrite` — the `overwrite` flag was present on the *first* call in this run but omitted on a later re-run that assumed `create` on an already-existing path would just work like the first time. Cause: my own error, not a tool bug — the CLI's behavior (create a numbered-suffix duplicate rather than error when the target exists and `overwrite` is absent) is standard "avoid clobbering" behavior, but it is a silent surprise if the caller doesn't expect it. Fix: always pass `overwrite` explicitly on every `obsidian create` call in Phase 7 once the file may already exist (i.e., any call after the first), and check `ls`/`obsidian file path=...` before assuming a re-run landed on the right target. Caught and cleaned up (duplicate deleted) before publishing, so no bad artifact reached the vault or the sync remote. Scope: global (SKILL.md's Phase 7 "Write the note via the Obsidian CLI" section should note this).
- [WORKFLOW] Evidence: The Phase 3 (library search) sub-agent dispatch failed on first attempt with a platform-level message ("claude-sonnet-5 is temporarily unavailable, so auto mode cannot determine the safety of Agent right now"). A retry with the identical dispatch (same `model: "haiku"`, same prompt) succeeded moments later with no changes on my end. Cause: transient Auto-mode safety-classifier outage, unrelated to the target model or the task. Fix: none needed beyond what SKILL.md already documents ("wait briefly and retry") — confirms the existing guidance is correct and sufficient. Scope: none (positive confirmation of existing guidance).
- [WORKFLOW] Evidence: This run's class is "sutta-anchored" (not "thematic"), so the Phase 2.5 and 3b gates did NOT auto-skip — Phase 3b (Sanskrit/GRETIL) required an explicit `scratch-log 3b ... --summary "Not applicable: ..."` call before `scratch-gate 3b` would accept it (the gate refused on first attempt with "no logged evidence"). Cause: correctly following the documented behavior — sutta-anchored runs need an explicit not-applicable log where a thematic run would have auto-skipped. Fix: none needed; this matches SKILL.md as written. Worth a one-line callout in SKILL.md's Phase 3b section for future orchestrators who might assume thematic-style auto-skip applies more broadly than it does. Scope: global (minor doc clarity, not a defect).
- [CONFUSION] Evidence: none this run beyond what's covered above.

## Improvement suggestions
- Suggest: In SKILL.md's Phase 6 section, add an explicit note that cross-check integration must re-verify *specific factual claims* (companion-sutta citations, parallel-relationship characterizations) against fresh tool calls, not just accept the reviewer's correction prose — this run did so correctly but the discipline is currently implicit rather than stated as its own sub-rule.
- Suggest: In SKILL.md's Phase 7 "Write the note via the Obsidian CLI" section, add a one-line warning that `obsidian create` without `overwrite` on an existing path creates a numbered-suffix duplicate rather than erroring — always pass `overwrite` explicitly on any Phase 7 re-write after the first.
- Suggest: In SKILL.md's spot-check guidance (referenced across Phases 2-4c), make explicit that sub-agent *summary sentences* must be checked against the raw JSON payload they claim to describe, not only the headline sutta/candidate selections — this run caught two such summary-prose drift errors (Phase 2's AN3.60 title/theme, Phase 4's MN51→EA43.8 false parallel claim).
- Suggest: In SKILL.md's Phase 3b section, add a one-line reminder that "sutta-anchored" class runs (unlike "thematic" runs) do not auto-skip the 2.5/3b gates and need an explicit not-applicable `scratch-log` call before gating.

## Channel tuning
- Promote to trusted: none this run (light-touch Phase 4b did not surface a channel warranting a tier change)
- Demote to excluded: none
- New probationary channels seen: Dhamma Audiobook Library, Buddhist World (both Snp 4.6 old-age material, probationary tier, titles sufficiently specific for citation without transcript fetch)

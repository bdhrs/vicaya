---
date: 2026-07-11
question_original: identify 10 lesser-known Early Buddhist discourses for 15-20 minute Dhamma talks to a mixed lay audience (beginners to advanced, Chinese heritage), with commentarial insights and Chinese Āgama parallels where available. Do not overlap with suttas from [[ten-lesser-known-suttas-for-mixed-lay-dhamma-talks]] and [[ten-lesser-known-suttas-generosity-right-livelihood]] and [[ten-more-lesser-known-suttas-generosity-right-livelihood]] and [[ten-more-lesser-known-suttas-for-mixed-lay-dhamma-talks]] and [[ten-more-lesser-known-suttas-mixed-lay-dhamma-talks-2]] and [[ten-more-lesser-known-suttas-mixed-lay-dhamma-talks-3]]
question_polished: Which ten lesser-known Early Buddhist discourses are well suited for 15–20 minute Dhamma talks to a mixed lay audience (beginners through advanced, Chinese heritage), with commentarial insights and Chinese Āgama parallels noted where available — without overlapping any of the sixty suttas already used in six prior notes in this series?
note_path: Vicaya/2026-07-11 - ten-more-lesser-known-suttas-mixed-lay-dhamma-talks-4.md
duration_min: 350
---

## Retrospective
- [BUG] Evidence: An early direct-SQL pull for AN4.55 (`SELECT ... FROM s0402m3_mul WHERE ... paranum='54'`) returned the *previous* sutta's body text (AN4.54 Dutiyasaṃvāsasuttaṃ, the "four cohabitations" corpse/goddess teaching) instead of AN4.55's actual content (Nakulapitā/Nakulamātā, samasaddhā-samasīlā-samacāgā-samapaññā). Cause: the CST database's title/subhead row for a new sutta is stamped with the *previous* sutta's final paranum, not its own — the new sutta's actual body starts at paranum+1. This is documented as Hard Rule 9 in SKILL.md but was still triggered live, because a direct SQL query is easy to write against the visually-labeled paranum without re-deriving the +1 offset each time. Fix: caught immediately by checking the retrieved content against what was expected (the "cohabitations" formula was obviously not about an elderly devoted couple's mutual future-life wish) and re-run with paranum='55'; the correct text was retrieved and used. No bad citation reached the final note. Scope: global — SKILL.md's Hard Rule 9 already documents the general pattern for `resolve-citation`, but could add a one-line callout that *direct SQL pulls* (not just `resolve-citation` calls) are equally subject to it, since this is the second time in this series a direct-SQL pull has been thrown off by it (the first was a paranum-repeats-per-section case for AN4.55's own commentary lookup, logged as an abandoned gap in this same run).
- [BUG] Evidence: An earlier Phase 4a web-search sub-agent (from before this session's context compaction) claimed AN2.120 ("two rare persons: benefactor and grateful repayer") was the same as or closely related to AN2.31–32 (the commonly anthologized "Kataññū Sutta"), and proposed an Access to Insight URL for AN2.31 as a citation for AN2.120. Checking both via `verify-citation` showed they resolve to different chapters entirely (AN2.120 in the *āsāduppajahavagga*; AN2.31/32 in the *bālavagga*/*samacittavagga*) with unrelated content. Cause: plausible-sounding sub-agent summary prose (similar surface theme — gratitude-adjacent) drifted from what the underlying citation actually supports. Fix: the mismatched citation was excluded from the final note; AN2.120's English rendering relies on the canon database translation only, and the correction is stated explicitly in the note's own Findings section (not silently dropped). Scope: this is the same failure mode flagged in the prior run's report (2026-07-11, Note 3) — "verify summary sentences against raw JSON, not just headline conclusions" — recurring a second time across two separate runs in this series, which strengthens the case for stating it as its own numbered SKILL.md sub-rule rather than leaving it implicit in general spot-check guidance.
- [BUG] Evidence: The `cross-check` helper (Phase 6) was invoked with the full ~380-line drafted note piped via stdin and `--timeout 260`. The underlying subprocess ran for 5 minutes 37 seconds — well past its own declared 260-second timeout — without producing any stdout or exiting, and had to be killed manually (`kill`, confirmed terminated via `ps`). This is a hang, not the documented graceful `# SELF_REVIEW:` fallback described in SKILL.md for when the cross-check chain fails. Cause: unclear from the orchestrator side — likely the `opencode`/`agy` subprocess in `VICAYA_CROSS_CHECK_CHAIN` blocked on something (network, auth prompt, or a very large-input parse) in a way its own `--timeout` argument did not actually enforce. Fix: fell back to a manual self-review, re-running `sc-parallels` directly against every one of the note's ten candidates and confirming every Āgama-parallel-present and Āgama-parallel-absent claim matched the raw tool output exactly (logged in full in the Phase 6 scratch section) before gating. This substitutes for, but is more time-consuming than, the intended automated pass. Scope: global — SKILL.md should note that the cross-check helper's internal `--timeout` is not a hard guarantee against a hung subprocess, and that the orchestrator should apply its own wall-clock budget (e.g. wait no more than ~2× the passed `--timeout` value before killing and falling back to manual self-review) rather than waiting indefinitely on the helper to self-terminate.
- [WORKFLOW] Evidence: A late-session Bash call (attempting the final AN8.54 future-benefits SQL pull, from before this session's context compaction) failed with a platform message: "claude-sonnet-5 is temporarily unavailable, so auto mode cannot determine the safety of Bash right now." A retry of the identical command in the next turn succeeded with no changes needed. Cause: transient Auto-mode safety-classifier outage, unrelated to the query itself. Fix: none needed beyond retrying, consistent with existing guidance from a prior run's report. Scope: none (positive confirmation of existing guidance, third occurrence noted across this series).
- [POSITIVE] Evidence: The mandatory per-phase scratch-write discipline (from this project's CLAUDE.md, not SKILL.md itself) worked as intended across the context-compaction boundary in this run — the conversation was compacted mid-session, and on resume every piece of Phase 0–4c evidence needed for Phase 5 drafting was already durably written to the scratch file rather than held only in conversation context, so no findings were lost and drafting could proceed immediately from `scratch-resume`/`scratch-verify` state. Cause: the discipline was followed correctly throughout Phases 2–4c in the pre-compaction portion of this run. Fix: none needed — confirms the project-level rule is working as designed for its stated purpose. Scope: none (positive confirmation).
- [CONFUSION] Evidence: none this run beyond what's covered above.

## Improvement suggestions
- Suggest: In SKILL.md's Hard Rule 9 (paragraph numbers are book-global), add an explicit one-line note that direct SQL pulls (not only `resolve-citation` calls) are subject to the same title-row-belongs-to-the-preceding-sutta pattern, and that the fix (query paranum+1 for the new sutta's actual body) applies equally there.
- Suggest: Promote the "verify sub-agent summary sentences against the raw JSON they claim to describe" guidance from implicit spot-check practice to an explicit, numbered SKILL.md rule — this is the second run in this series (after 2026-07-11 Note 3's MN51→EA43.8 error) where a sub-agent's plausible-sounding summary prose about a citation/parallel relationship was found to be false only because the orchestrator independently re-checked the raw tool output rather than trusting the summary.
- Suggest: In SKILL.md's Phase 6 section, add guidance that the `cross-check` helper's internal `--timeout` should not be trusted as a hard ceiling — the orchestrator should independently bound how long it waits on the subprocess (e.g. ~2× the passed timeout) before killing it and falling back to the documented manual self-review checklist, since this run observed the subprocess hang well past its declared timeout without exiting or producing the `# SELF_REVIEW:` sentinel.

## Channel tuning
- Promote to trusted: none this run
- Demote to excluded: none
- New probationary channels seen: พุทธะศาลา (Thai-language channel, UD2.8/Suppavāsā content, title-level confirmation only, probationary tier — not fetched for transcript since content would need translation verification beyond this run's scope)

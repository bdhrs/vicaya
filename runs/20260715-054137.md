---
date: 2026-07-15
question_original: Bhava
question_polished: What do the Early Buddhist Texts say about Bhava?
note_path: Vicaya/What the Suttas Say About/2026-07-15 - what-the-suttas-say-about-bhava.md
duration_min: ~55
---

## Retrospective
- [POSITIVE] Evidence: The compound-driven canon search strategy worked very well — searching `bhavanirodha`, `bhavataṇhā`, `kāmabhava`, `upādānapaccayā bhavo`, `tisso/tayo bhavā`, `bhavaṅga` etc. as precise compounds (rather than the bare stem `bhav`, which is drowned by verbal "to be" forms) cleanly surfaced every doctrinal locus. Cause: the stem-truncation rule is right for single words but catastrophic for `bhav`-rooted verbs. Fix: preserve this compound-first approach for any term whose root is also a high-frequency verb. Scope: local.
- [POSITIVE] Evidence: The `kāmabhavo` substring search in s0302m_mul recovered the SN12.2 Vibhaṅga definition ("Tayo me, bhikkhave, bhavā...") that the `tayo bhavā` and `tisso bhavā` phrase searches both missed — because the canonical text inserts `me, bhikkhave,` between `tayo` and `bhavā`. Cause: fixed-phrase search is brittle to inserted vocatives/particles. Fix: when a definitional formula is expected but phrase-search returns 0, fall back to a distinctive single compound from the expected definition (here `kāmabhavo`) scoped to the target book. Scope: local.
- [WORKFLOW] Evidence: The cross-check helper returned the `# SELF_REVIEW:` sentinel — VICAYA_CROSS_CHECK_CHAIN is empty or opencode/agy aren't authenticated on this machine. Cause: no cross-check chain configured. Fix: either configure the chain in .env or document that self-review is the default on this host. Scope: global. Suggest: note in SKILL that self-review is the expected path when the chain is unconfigured, so it doesn't feel like a failure.
- [BEHAVIOR] Evidence: I nearly skipped Phase 6 (cross-check) — went from Phase 5 synthesis straight to drafting and Phase 7, and only caught the omission when answering self-audit question 6. Cause: the Phase 5→6→7 sequence isn't enforced by a gate between synthesis and cross-check. Fix: consider making scratch-gate 7 refuse if gate 6 is missing AND no cross-check log entry exists (it may already — I caught it manually before gating 7). Scope: global.
- [DOC] Evidence: The skill's sub-agent dispatch section assumes Claude Code's Agent tool or an external CLI sub-agent with VICAYA_PHASE pinning, which doesn't map to pi's named-agent `subagent` tool. I ran gather phases inline instead. Cause: the skill is written for Claude-Code-style harnesses. Fix: add a one-paragraph note that in harnesses without a phase-pinnable sub-agent mechanism, running gather phases inline with --quiet + scratch-dossier reliance is an acceptable fallback, and state it in the run reflection. Scope: global.

## Improvement suggestions
- Suggest: Document in the SKILL's Phase 2 section that for terms whose Pāḷi root is also a high-frequency verb (bhav, hot, atth, gacch), compound-search is mandatory and stem-search is counter-productive — generalise the lesson from this run.
- Suggest: Add a note that when a definitional formula phrase-search returns 0 in a book where the definition is expected, retry with a single distinctive compound from the expected definition scoped to that book (the `tayo me ... bhavā` insertion case).
- Suggest: Clarify the inline-vs-sub-agent decision for non-Claude-Code harnesses (pi, etc.) so agents don't attempt a sub-agent dispatch that can't pin VICAYA_PHASE.
- Suggest: Confirm whether scratch-gate 7 already enforces a Phase 6 cross-check log entry; if not, add it as a guard against the "skip Phase 6" failure mode I nearly hit.

## Channel tuning
- Promote to trusted: none
- Demote to excluded: none
- New probationary channels seen: Maha Bodhi Society Bengaluru, Innovative Dhamma, CRYSTAL (all surfaced on bhava/becoming queries; not promoted — content unverified)
